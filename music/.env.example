### ========================================
### Music ENV Example
### ========================================

# -------------------------
# Global Settings
# -------------------------

# Docker restart policy:
# 'no' | 'always' | 'unless-stopped' | 'on-failure'
RESTART_POLICY=unless-stopped

# IP address the containers will bind to (0.0.0.0 binds to all interfaces)
BIND_ADDRESS=0.0.0.0

# Timezone for your containers
TZ=Europe/London

# User and group IDs for file permissions
# Make sure these match your host system user/group
PUID=1000
PGID=1000

# Paths for persistent storage
# These should be absolute paths on your host
CONFIG_DIR=/config       # Config files and metadata
DATA_DIR=/data           # Music library and downloads

# -------------------------
# Lidarr Settings
# -------------------------

# Add Lidarr-specific environment variables here
# For example:
# LIDARR_PORT=8686
# LIDARR_API_KEY=your_api_key_here
